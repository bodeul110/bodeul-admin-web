# 브라우저에 공개되는 Firebase Web config
NEXT_PUBLIC_FIREBASE_API_KEY=<firebase-web-api-key>
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=<firebase-auth-domain>
NEXT_PUBLIC_FIREBASE_PROJECT_ID=<firebase-project-id>
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=<firebase-storage-bucket>
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=<firebase-messaging-sender-id>
NEXT_PUBLIC_FIREBASE_APP_ID=<firebase-web-app-id>

# 브라우저 provider 스위치. Production은 true, 긴급 rollback은 false로 바꾼 뒤 재배포한다.
NEXT_PUBLIC_FIREBASE_APPCHECK_ENABLED=false

# 브라우저 공개값. Firebase에 등록한 reCAPTCHA Enterprise score key를 사용한다.
NEXT_PUBLIC_FIREBASE_APPCHECK_SITE_KEY=

# 로컬 전용. App Check debug token은 Preview/Production에 넣지 않는다.
NEXT_PUBLIC_FIREBASE_APPCHECK_DEBUG_TOKEN=

# Next.js 기본값은 api + 동일 출처다. 외부 Node API 비교가 필요할 때만 URL을 설정한다.
NEXT_PUBLIC_BODEUL_DATA_BACKEND=api
NEXT_PUBLIC_BODEUL_API_BASE_URL=

# 서버 전용. Supabase transaction pooler(6543)의 bodeul_admin_service URL을 사용한다.
ADMIN_DATABASE_URL=<postgresql-transaction-pooler-url>
FIREBASE_PROJECT_ID=<firebase-project-id>
FIREBASE_STORAGE_BUCKET=<firebase-storage-bucket>

# 서버의 Firestore·Storage 중계 route에서만 사용한다. Preview/Production 값을 분리하고 브라우저에 노출하지 않는다.
FIREBASE_SERVICE_ACCOUNT_JSON=<single-line-service-account-json>

# off | observe | enforce. 배포 초기값은 observe이며 VALID 요청 확인 전에는 enforce하지 않는다.
ADMIN_APP_CHECK_MODE=observe

# off | observe | enforce. 모든 운영 관리자 MFA 등록과 재로그인 확인 뒤 enforce로 전환한다.
ADMIN_MFA_MODE=observe

# 모든 운영 관리자에게 SMS 또는 TOTP가 등록되고 복구 절차를 확인한 뒤에만 true로 바꾼다.
ADMIN_MFA_ENFORCE_READY=false

# 현재 환경에서 허용할 Firebase Web App ID. 여러 값은 쉼표로 구분한다.
FIREBASE_APPCHECK_ALLOWED_APP_IDS=<firebase-web-app-id>

# Vite rollback은 아래 기존 이름도 지원한다. 서버 비밀값은 VITE_ 이름으로 만들지 않는다.
VITE_FIREBASE_API_KEY=<firebase-web-api-key>
VITE_FIREBASE_AUTH_DOMAIN=<firebase-auth-domain>
VITE_FIREBASE_PROJECT_ID=<firebase-project-id>
VITE_FIREBASE_STORAGE_BUCKET=<firebase-storage-bucket>
VITE_FIREBASE_MESSAGING_SENDER_ID=<firebase-messaging-sender-id>
VITE_FIREBASE_APP_ID=<firebase-web-app-id>
VITE_FIREBASE_APPCHECK_ENABLED=false
VITE_FIREBASE_APPCHECK_SITE_KEY=
VITE_FIREBASE_APPCHECK_DEBUG_TOKEN=
VITE_BODEUL_DATA_BACKEND=firebase
VITE_BODEUL_API_BASE_URL=
